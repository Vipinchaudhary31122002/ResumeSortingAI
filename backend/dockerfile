FROM node:23-slim

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Serve built frontend from /app/frontend (from mounted volume)
# Your backend should statically serve from this directory
# Example (Express):
# app.use(express.static(path.join(__dirname, 'frontend')));

EXPOSE 8080

CMD ["npm", "start"]
